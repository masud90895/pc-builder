/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['via.placeholder.com', 'cdn.dribbble.com', 'www.pngkey.com', 'i.ibb.co'],
  }
}

module.exports = nextConfig
